scalar Date

type Query {
  product_categories: [Product_Category]
  product_categoryById(id: ID!): Product_Category
}

type Product_Category {
  id: ID!
  name: String
  slug: String
  created_by: Int
  updated_by: Int
  created_at: Date
  updated_at: Date
}
